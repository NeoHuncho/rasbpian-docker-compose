services:
  gamehub:
    image: ghcr.io/host-it-labs/gamehub:latest
    restart: unless-stopped
    init: true
    ports:
      - '${GAMEHUB_BIND:-127.0.0.1}:${GAMEHUB_PORT:-8080}:8080'
    environment:
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:?Set PUBLIC_ORIGIN to the public Gamehub URL}
      DATABASE_PATH: /data/gamehub.sqlite
    volumes:
      - gamehub-data:/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
volumes:
  gamehub-data:
